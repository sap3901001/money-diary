 1. 定義顏色變數 - 方便日後微調 
root {
     背景與卡片 
    --bg-color #f2f0f7;           極淺薰衣草灰 (背景) 
    --card-bg #ffffff;            純白卡片 
    --nav-bg #d8d6e5;             莫蘭迪灰紫色 (導航列) 
    
     文字顏色 
    --text-main #4a4a4a;          深灰色文字 
    --text-muted #8e8e8e;         淺灰色說明文字 
    
     元件顏色 (馬卡龍粉彩色系) 
    --color-expense #e59a8d;      乾燥玫瑰粉 (支出) 
    --color-income #a3c6e4;       寶寶藍 (收入) 
    --color-primary-btn #7d6da3;  深丁香紫 (新增按鈕) 
    --color-border #e0e0e0;       邊框顏色 
    
     陰影與圓角 
    --border-radius 12px;
    --box-shadow 0 4px 15px rgba(0, 0, 0, 0.05);
}

 2. 全域樣式設定 
body {
    background-color var(--bg-color);
    font-family PingFang TC, Microsoft JhengHei, sans-serif;
    color var(--text-main);
    margin 0;
}

 3. 導航列樣式 
.navbar {
    background-color var(--nav-bg) !important;
    padding 10px 20px;
    box-shadow var(--box-shadow);
}

.navbar-brand {
    font-weight bold;
    color var(--text-main) !important;
}

 4. 卡片容器 
.card {
    background-color var(--card-bg);
    border none;
    border-radius var(--border-radius);
    box-shadow var(--box-shadow);
    margin-bottom 25px;
    padding 20px;
}

 5. 支出收入 切換按鈕組 
.btn-group-toggle {
    display flex;
    gap 0;
    border-radius 8px;
    overflow hidden;
}

.btn-expense {
    background-color var(--color-expense);
    color white;
    border none;
    flex 1;
    padding 12px;
}

.btn-income {
    background-color var(--color-income);
    color white;
    border none;
    flex 1;
    padding 12px;
}

 6. 輸入欄位樣式 
.form-control, .form-select {
    border 1px solid var(--color-border);
    border-radius 8px;
    padding 10px;
    background-color #fafafa;
}

.form-controlfocus {
    border-color var(--color-primary-btn);
    box-shadow 0 0 0 0.2rem rgba(125, 109, 163, 0.25);
}

 7. 主提交按鈕 (Add) 
.btn-add {
    background-color var(--color-primary-btn);
    color white;
    border none;
    border-radius 8px;
    width 100%;
    padding 12px;
    font-size 1.1rem;
    transition filter 0.3s;
}

.btn-addhover {
    filter brightness(1.1);
}

 8. 記帳清單樣式 
.record-item {
    display flex;
    justify-content space-between;
    align-items center;
    padding 12px 0;
    border-bottom 1px solid #f0f0f0;
}

.badge-expense {
    background-color var(--color-expense);
    color white;
    padding 4px 8px;
    border-radius 6px;
    font-size 0.8rem;
    margin-right 10px;
}

.amount-text {
    font-weight 500;
    color var(--color-expense);  預設支出顏色，收入可改用 var(--color-income) 
}